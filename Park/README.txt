Park Folder Contains all the 7 php files and test.sql file

It also contains conn.php file where you enter your hostname,username,password,database_name

create two tables one is box with only one attribute called 'slotno' of type int
and second one is pa with 'regno','slotno','date' of type int,int and timestamp respectively.  